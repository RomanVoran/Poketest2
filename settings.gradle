dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // for GlideSvg
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Poketest2"
include ':app'
